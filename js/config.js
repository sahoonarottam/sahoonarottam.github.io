// Site configuration — update these before deploying
window.SITE_CONFIG = {
  // GitHub repo where blog posts live (used to fetch markdown via GitHub API)
  // Update these two lines once the repo is created on GitHub.
  githubOwner: 'sahoonarottam',
  githubRepo: 'sahoonarottam',
  githubBranch: 'main',

  // Path inside the repo where posts are stored
  postsPath: 'content/posts',

  // Author display
  authorName: 'Dr. Narottam Sahoo',
};
